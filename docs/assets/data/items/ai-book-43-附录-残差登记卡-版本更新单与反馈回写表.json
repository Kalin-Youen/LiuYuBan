{"id":"ai-book-43-附录-残差登记卡-版本更新单与反馈回写表","html":"<p>﻿# 附录 残差登记卡、版本更新单与反馈回写表</p>\n<p>这一附录不是理论章节，而是供作者、AI 与读者在后续协作中直接使用的工作模板。它的目标很简单：让“随手评论、残差、版本、反馈、修改边界与触发判断”这几类最容易在扩写中丢失的东西，获得统一写法。</p>\n<h2 id=\"一残差登记卡\">一、残差登记卡</h2>\n<p>适用情形：</p>\n<ul>\n<li>某个问题反复出现但尚不能并入主线；</li>\n<li>某段扩写必须降强，不能直接写成结论；</li>\n<li>某类读者误解持续出现，但还没有专门接口处理。</li>\n</ul>\n<p>最小模板：</p>\n<pre><code class=\"language-text\">残差名称：\n出现位置：\n所属主线节点：\n问题类型：\n当前状态：\n当前处理动作：\n建议回写位置：\n下一步条件：\n关联章节 / 术语：\n备注：\n</code></pre>\n<p>建议填写说明：</p>\n<ul>\n<li><code>残差名称</code> 用一句短语命名，不要写成整段说明；</li>\n<li><code>出现位置</code> 标出章节、专题、站点入口或反馈来源；</li>\n<li><code>问题类型</code> 可选：术语漂移 / 推导缺桥 / 边界过宽 / 自我失真风险 / 反馈噪声 / 待验证接口；</li>\n<li><code>当前状态</code> 可选：立即回看 / 本轮待处理 / 持续观察 / 可自然耗散；</li>\n<li><code>当前处理动作</code> 可选：吸收 / 降强 / 转专题 / 暂挂 / 删除；</li>\n<li><code>下一步条件</code> 必须写清楚它需要什么材料、证据或章节承接。</li>\n</ul>\n<h2 id=\"二版本更新单\">二、版本更新单</h2>\n<p>适用情形：</p>\n<ul>\n<li>主线发生补桥、收紧或重写；</li>\n<li>一个核心词改变了职责；</li>\n<li>新增章节、专题或入口改变了整体阅读路径；</li>\n<li>需要保证改坏后可回滚。</li>\n</ul>\n<p>最小模板：</p>\n<pre><code class=\"language-text\">更新名称：\n更新时间：\n父版本 / 快照编号：\n变更类型：\n涉及文件：\n变更摘要：\n旧写法失效点：\n新写法保留点：\n回滚锚点：\n需要同步回看的位置：\n读者可见说明：\n入口 / 术语同步情况：\n关联残差：\n</code></pre>\n<p>建议填写说明：</p>\n<ul>\n<li><code>父版本 / 快照编号</code> 用来说明这次修改是从哪一版长出来的；</li>\n<li><code>变更类型</code> 可选：主线变更 / 边界变更 / 接口变更 / 残差变更 / 入口变更；</li>\n<li><code>变更摘要</code> 应压成一到三句，说明这次到底改了哪一层结构；</li>\n<li><code>旧写法失效点</code> 用来防止系统继续沿旧主线扩写；</li>\n<li><code>新写法保留点</code> 用来说明这次不是纯否定，而是重写后的新稳定点；</li>\n<li><code>回滚锚点</code> 应写清楚旧稿位置、快照文件或版本标签；</li>\n<li><code>读者可见说明</code> 至少要让常读者知道“哪里改了、为什么改、旧版是否仍可回看”；</li>\n<li><code>需要同步回看的位置</code> 至少写出术语表、总结章、生活卷或站点入口中受影响的部分。</li>\n</ul>\n<h2 id=\"三回写修改任务单\">三、回写修改任务单</h2>\n<p>适用情形：</p>\n<ul>\n<li>某条评论明显只打在局部段落，不应把整章一起重写；</li>\n<li>准备让 AI 协助修改，但必须先锁定范围和幅度；</li>\n<li>需要保护核心骨架不被顺手改散。</li>\n</ul>\n<p>最小模板：</p>\n<pre><code class=\"language-text\">任务名称：\n触发来源：\n命中章节 / 段落：\n本轮目标：\n允许修改范围：\n禁止改动骨架：\n改动上限：\n必须携带的锚点材料：\n是否需要先留快照：\n完成后必须生成的附带对象：\n备注：\n</code></pre>\n<p>建议填写说明：</p>\n<ul>\n<li><code>触发来源</code> 标出对应评论、生长卡、反馈表或残差卡；</li>\n<li><code>本轮目标</code> 只写这次最想解决的一个主问题，不要把所有不满一起塞进来；</li>\n<li><code>允许修改范围</code> 尽量写成一句 / 一段 / 一小节 / 只补桥不重排；</li>\n<li><code>禁止改动骨架</code> 写明哪些核心命名、章节顺序或论证链暂时不能碰；</li>\n<li><code>改动上限</code> 可选：措辞修补 / 局部降强 / 局部补桥 / 小节级调整 / 需升级为版本级改写；</li>\n<li><code>必须携带的锚点材料</code> 至少写清全书摘要、术语表、写作风格说明里哪些必须带上；</li>\n<li><code>完成后必须生成的附带对象</code> 可选：版本单 / 公开变更说明 / 残差卡 / 入口同步单。</li>\n</ul>\n<h2 id=\"四评论筛选与冲突归并表\">四、评论筛选与冲突归并表</h2>\n<p>适用情形：</p>\n<ul>\n<li>评论很多，不能把每一条都直接喂给 AI；</li>\n<li>多条评论看起来都在说问题，但其实质量差异很大；</li>\n<li>彼此矛盾的评论同时出现，需要先分清再决定是否回写。</li>\n</ul>\n<p>最小模板：</p>\n<pre><code class=\"language-text\">评论来源：\n原始评论：\n质量判断：\n命中问题：\n是否与其他评论同向汇聚：\n是否与其他评论形成冲突：\n冲突组编号：\n建议处理动作：\n是否需要人工复核：\n备注：\n</code></pre>\n<p>建议填写说明：</p>\n<ul>\n<li><code>质量判断</code> 可选：建设性 / 待观察 / 灌水 / 恶意风险；</li>\n<li><code>命中问题</code> 用一到两句压清，不要原样堆评论；</li>\n<li><code>同向汇聚</code> 用来判断这是不是多个读者都在暴露同一缺口；</li>\n<li><code>形成冲突</code> 用来防止 AI 把彼此相反的意见平均成一句模糊话；</li>\n<li><code>建议处理动作</code> 可选：直接进入生长卡 / 合并同类 / 暂挂观察 / 忽略 / 人工复核；</li>\n<li><code>是否需要人工复核</code> 在恶意风险高、问题牵涉主线、或冲突过强时应写“是”。</li>\n</ul>\n<h2 id=\"五读者反馈回写表\">五、读者反馈回写表</h2>\n<p>适用情形：</p>\n<ul>\n<li>收到值得保留的读者反馈；</li>\n<li>发现某一段虽然读懂了，但没有留下判断路径；</li>\n<li>某个章节引发稳定误解或稳定影响。</li>\n</ul>\n<p>最小模板：</p>\n<pre><code class=\"language-text\">反馈来源：\n读者类型：\n命中章节：\n反馈类型：\n反馈内容压缩：\n命中点判断：\n反馈权重：\n是否进入判断：\n建议动作：\n回写位置：\n是否新增残差 / 版本 / 公开说明：\n</code></pre>\n<p>建议填写说明：</p>\n<ul>\n<li><code>反馈类型</code> 可选：进入困难 / 误解点 / 无感区 / 高影响段 / 新接口需求；</li>\n<li><code>反馈内容压缩</code> 要压成一到三句，不要原样堆评论；</li>\n<li><code>命中点判断</code> 说明它到底打在定义、主线、边界、入口还是表达顺序上；</li>\n<li><code>反馈权重</code> 可选：低 / 中 / 高；高权重通常对应主线误解、反复卡点或现实接口强反馈；</li>\n<li><code>是否进入判断</code> 用来区分“读者只是看懂了”还是“读者真的开始调用这套尺度”；</li>\n<li><code>建议动作</code> 可选：改导读 / 补桥 / 降强 / 转专题 / 登记残差；</li>\n<li><code>是否新增残差 / 版本 / 公开说明</code> 用来提醒这次反馈是否已经触到结构级修改。</li>\n</ul>\n<h2 id=\"六独立评审处理单\">六、独立评审处理单</h2>\n<p>适用情形：</p>\n<ul>\n<li>收到来自外部 AI、独立读者或整站体验者的长篇评审；</li>\n<li>一份评审里同时混着入口、语气、内容、站点和协作流程问题；</li>\n<li>需要防止“有价值的批评”和“没读到就推断的判断”被平均处理。</li>\n</ul>\n<p>最小模板：</p>\n<pre><code class=\"language-text\">评审来源：\n评审时间：\n实际访问范围：\n未访问 / 未验证范围：\n有效反馈压缩：\n最强化质疑：\n命中点：\n处理动作：\n主回写位置：\n需要同步的次级位置：\n暂不采纳项及原因：\n是否生成残差 / 版本 / 公开说明：\n备注：\n</code></pre>\n<p>建议填写说明：</p>\n<ul>\n<li><code>实际访问范围</code> 只记对方真的读到的页面、章节或功能，不替它补读；</li>\n<li><code>未访问 / 未验证范围</code> 要单独列出，防止系统把“没看到”误写成“已证伪”；</li>\n<li><code>有效反馈压缩</code> 最好压成三到五条可执行问题，而不是整段转贴原评审；</li>\n<li><code>最强化质疑</code> 用来把原话改写成真正需要处理的硬问题；</li>\n<li><code>命中点</code> 可选：定义 / 主线 / 边界 / 入口 / 站点交互 / 协作流程；</li>\n<li><code>处理动作</code> 可选：吸收 / 补桥 / 降强 / 转专题 / 登记残差 / 明确拒绝；</li>\n<li><code>暂不采纳项及原因</code> 至少区分三类：对方没读到、命中点不准、当前证据不足；</li>\n<li>若评审已经触发结构更新，应连带生成版本更新单或公开变更说明。</li>\n</ul>\n<h2 id=\"七活书触发判断单\">七、活书触发判断单</h2>\n<p>适用情形：</p>\n<ul>\n<li>不确定这一轮到底该不该改；</li>\n<li>评论数量不多，但命中问题很重；</li>\n<li>热门章节改得太频繁，冷门章节长期不动。</li>\n</ul>\n<p>最小模板：</p>\n<pre><code class=\"language-text\">命中章节：\n观察时间窗：\n本轮评论数量：\n重复命中问题：\n高权重反馈是否出现：\n距离上次修改间隔：\n上一版是否已观察充分：\n本轮触发方式：\n是否进入修改：\n若不进入，下一次回看时间：\n若进入，必须先生成的对象：\n备注：\n</code></pre>\n<p>建议填写说明：</p>\n<ul>\n<li><code>重复命中问题</code> 用来判断这是不是同类缺口反复显影；</li>\n<li><code>高权重反馈是否出现</code> 主要看是否直接打在主线、边界、误解点与现实接口上；</li>\n<li><code>本轮触发方式</code> 可选：按期 / 按量 / 按权重 / 三者并行；</li>\n<li><code>是否进入修改</code> 不应只看数量，也要看上一版是否已经给够观察时间；</li>\n<li><code>必须先生成的对象</code> 通常至少包括：快照、修改任务单、必要时的版本单。</li>\n</ul>\n<h2 id=\"八最小使用节奏\">八、最小使用节奏</h2>\n<p>如果不想把这几张表用得太重，可以采用一个最小节奏：</p>\n<ol>\n<li>先把随手评论压成<code>评论生长卡</code>；</li>\n<li>先用<code>评论筛选与冲突归并表</code>把建设性反馈、灌水和冲突分开；</li>\n<li>收到长篇独立评审时，先补一张<code>独立评审处理单</code>，再决定要不要拆成反馈表、残差卡或任务单；</li>\n<li>每轮准备真的动正文前，先补一张<code>活书触发判断单</code>；</li>\n<li>一旦决定回写，再先补一张<code>回写修改任务单</code>；</li>\n<li>遇到不能立即吸收的问题，就补一张<code>残差登记卡</code>；</li>\n<li>收到值得保留的读者反馈，就压成<code>反馈回写表</code>；</li>\n<li>一旦正式改动结构，再生成<code>版本更新单</code>与必要的公开说明；</li>\n<li>每经过一轮较大重写，再统一回看一次这些表之间是否已经形成回路。</li>\n</ol>\n<p>这套节奏的目标，不是增加管理动作，而是让重要变化不再只留在聊天记录和个人记忆里。</p>\n<h2 id=\"九这些表之间怎样连接\">九、这些表之间怎样连接</h2>\n<p>这几张表不是并列孤立的。更稳的连接方式是：</p>\n<ol>\n<li>随手评论先进入<code>评论生长卡</code>；</li>\n<li>评论过多时，先经过<code>评论筛选与冲突归并表</code>；</li>\n<li>长篇独立评审先进入<code>独立评审处理单</code>，先把已读范围、有效问题和暂不采纳项分开；</li>\n<li>真要动正文前，先过一张<code>活书触发判断单</code>；</li>\n<li>确认要改后，再补<code>回写修改任务单</code>锁定范围与上限；</li>\n<li>其中当前不能直接吸收的，转入<code>残差登记卡</code>；</li>\n<li>其中来自读者实际接收结果的，继续压成<code>反馈回写表</code>；</li>\n<li>一旦正式改动结构，再生成<code>版本更新单</code>；</li>\n<li>版本更新后，再反过来修正导读、术语和站点入口。</li>\n</ol>\n<p>也就是说：</p>\n<blockquote>\n<p>评论生长卡负责把碎片带进来，筛选表负责把噪声与冲突分开，独立评审处理单负责把长评审拆成可执行问题，触发表负责决定“现在该不该改”，任务单负责锁定“这次最多改到哪里”，残差负责让暂时吸收不了的问题不丢失，反馈表负责显影实际接收结果，版本单负责让改动留下结构痕迹。</p>\n</blockquote>\n<h2 id=\"十随手评论生长卡\">十、随手评论生长卡</h2>\n<p>适用情形：</p>\n<ul>\n<li>群聊、备忘、语音转写里出现了零散但可能有价值的想法；</li>\n<li>作者知道这里“像是有东西”，但还远不到正文强度；</li>\n<li>一段评论里混着误解、补桥、例子、动机和情绪，不宜直接扩写。</li>\n</ul>\n<p>最小模板：</p>\n<pre><code class=\"language-text\">原始评论：\n压缩表达：\n质量判断：\n评论类型：\n冲突关系：\n命中点：\n建议修改范围：\n改动上限：\n建议动作：\n建议去向与锚点：\n可信状态：\n是否转残差 / 反馈 / 版本 / 公开说明：\n备注：\n</code></pre>\n<p>建议填写说明：</p>\n<ul>\n<li><code>原始评论</code> 尽量保留原句，不急着润色；</li>\n<li><code>压缩表达</code> 用一到两句把它压成可判断的话；</li>\n<li><code>质量判断</code> 可选：建设性 / 待观察 / 灌水 / 恶意风险；</li>\n<li><code>评论类型</code> 可选：术语提醒 / 误解反馈 / 桥接需求 / 现实接口 / 新例子 / 动机方向 / 噪声待观察；</li>\n<li><code>冲突关系</code> 说明它是与哪些评论同向汇聚，还是与哪些评论形成对冲；</li>\n<li><code>命中点</code> 可选：定义 / 主线 / 边界 / 入口 / 专题 / 协作流程；</li>\n<li><code>建议修改范围</code> 尽量写清一句 / 一段 / 小节 / 暂不动正文；</li>\n<li><code>改动上限</code> 可选：措辞修补 / 局部降强 / 局部补桥 / 小节级调整 / 需升级为版本级改写；</li>\n<li><code>建议动作</code> 可选：吸收 / 补桥 / 降强 / 转专题 / 登记残差 / 自然耗散；</li>\n<li><code>建议去向与锚点</code> 标出主书稿、生活卷、专题、AI 协作卷、附录或站点入口，并写清必须带上的摘要、术语表或风格说明；</li>\n<li><code>可信状态</code> 可选：已较清楚 / 猜测 / 材料不足；</li>\n<li><code>是否转残差 / 反馈 / 版本 / 公开说明</code> 用来说明这条评论下一步究竟进入哪套正式结构。</li>\n</ul>\n<p>最小投喂提示词：</p>\n<pre><code class=\"language-text\">下面是一些随手评论，请不要直接替我扩写成正文。\n请先把它们拆成若干条评论生长卡。\n每张卡都要给出：原始评论、压缩表达、质量判断、评论类型、冲突关系、命中点、建议修改范围、改动上限、建议动作、建议去向与锚点、可信状态。\n拿不准的地方请明确写“猜测”或“材料不足”。\n最后请额外指出：哪些卡值得转残差，哪些卡值得直接形成补桥段落或新专题，哪些卡应该被过滤。\n</code></pre>\n<h2 id=\"十一这一附录的压缩结论\">十一、这一附录的压缩结论</h2>\n<p>若把这份附录压成一句最实用的话，那就是：</p>\n<blockquote>\n<p>凡随手冒出来但可能有价值的，先压成评论生长卡；凡评论太多或彼此矛盾的，先做筛选与归并；凡收到长篇独立评审的，先补独立评审处理单；凡准备真的动正文的，先判断触发条件，再锁定修改边界；凡不能立即并入主线的，就登记残差；凡已经改变结构的，就留版本与公开说明；凡来自读者现实接收的，就再压成可回写反馈，而不是让它们散在对话和记忆里。</p>\n</blockquote>"}